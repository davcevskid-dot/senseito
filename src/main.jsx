import React from "react";
import { createRoot } from "react-dom/client";
import Senseito from "./Senseito.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Senseito />
  </React.StrictMode>
);
